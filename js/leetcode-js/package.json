{
  "name": "leetcode",
  "version": "1.0.0",
  "description": "Bennet's leetcode sandbox",
  "main": "index.ts",
  "scripts": {
    "clean": "rm -rf coverage build tmp",
    "start": "node build/index.js",
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "build:release": "npm run clean && tsc -p tsconfig.release.json",
    "lint": "eslint .",
    "test": "npm run build && npm start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@types/node": "^22.13.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  }
}
